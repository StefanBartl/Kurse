# TODO

# Get user input via python's built-in input() function, which gives a str back
name = input("What is your name? ")
# Print out the user name with python's built in print() function, where we can format with f to get the literal str possibility with {}
print(f"hello, {name}")